language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# Travis CI Server Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

before_deploy:
  - zip -r shareyourtrip *
  - mkdir -p deploy
  - mv shareyourtrip.zip deploy/shareyourtrip.zip

deploy :
  - provider: s3
    access_key_id: $AWS_ACCESS_KEY # Travis Environment Variables
    secret_access_key: $AWS_SECRET_KEY # Travis Environment Variables
    bucket: shareyourtrip-springboot-build
    region: ap-northeast-2
    skip_cleanup: true
    acl: private # zip 파일 접근 private
    local_dir: deploy # before deploy 에 설정한 디렉토리
    wait-until-deploed: true

# CI 실행 완료 시 메일 알림
notifications:
  email:
    recipients:
      - jbb9229@gmail.com